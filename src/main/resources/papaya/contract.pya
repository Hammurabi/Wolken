struct name {
    public Field field
    fn testFun() {
        fun()
        Field genericDeclaration = 0
        let generic = someValue
        let lambda_test = (type a, type b)=>{
        }
        let x, y, z = x(x, y, z)
    }
}